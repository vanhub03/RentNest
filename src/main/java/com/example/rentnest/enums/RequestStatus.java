package com.example.rentnest.enums;

public enum RequestStatus {
    PENDING,    // Đang chờ duyệt
    APPROVED,   // Đã duyệt (Tạo HĐ nháp)
    REJECTED,   // Bị từ chối
    CANCELLED   // Khách hủy
}
